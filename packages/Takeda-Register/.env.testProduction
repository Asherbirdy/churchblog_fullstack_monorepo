# 基礎路徑(VITE_BASE變數多處出現 請勿更改名稱) https://cdn.clicugo.com/TakedaTest/register/

# 
# 
# 
# ! CDN 會有CORS問題 所以現在不用 .env.testProduction （ 不會顯示 select option api）
# ! CDN 會有CORS問題 所以現在不用 .env.testProduction
# ! CDN 會有CORS問題 所以現在不用 .env.testProduction
# ! CDN 會有CORS問題 所以現在不用 .env.testProduction

VITE_BASE=/TakedaTest/register/
VITE_API=https://uat-takeda-lab.riversoft.com.tw
VITE_ENVIRONMENT=testProduction

VITE_PORT=1234